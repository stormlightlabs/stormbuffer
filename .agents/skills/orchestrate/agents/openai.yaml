interface:
  display_name: "Orchestrate with Pi"
  short_description: "Delegate work or run paired Pi reviews"
  default_prompt: "Use $orchestrate to delegate this task, using paired standard and adversarial Pi reviewers when review is needed."
